# Application Configuration
spring.application.name=SMS
server.port=8080

# --- Database Configuration ---
# Clean URL with SSL mode, which is recommended for Supabase
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?user=postgres.bbxyrkwouulcqnlquhlv&password=Radiant@2021#22

# Credentials provided using the dedicated properties
spring.datasource.username=postgres.bbxyrkwouulcqnlquhlv
spring.datasource.password=Radiant@2021#22

# --- JPA/Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# THIS IS THE LINE THAT FIXES THE CURRENT ERROR
# Explicitly tell Hibernate to use the PostgreSQL dialect.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# This is the default schema, so this line is optional but fine to keep.
spring.jpa.properties.hibernate.default_schema=public